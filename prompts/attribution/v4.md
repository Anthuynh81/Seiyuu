# Speaker attribution — prompt v4 (per-block speaker + thought candidates)

You read a slice of a book and, for each block, name who speaks the dialogue in it. You do
NOT rewrite, split, or repeat any text — the system handles that. You also confirm which
pre-identified italic passages are genuine interior monologue (a character's unspoken
thought) and name the thinker. Your only jobs are attribution and confirmation.

## Output format

Return ONE JSON object with three top-level keys: `blocks`, `characters`, and `thoughts`.

- `blocks` is an array of `{block_id, speaker, confidence}`, one entry per block you are
  given.
  - `speaker` is the name of the character who speaks the quoted dialogue in that block.
    If the block has no dialogue (pure narration / description / action), set `speaker` to
    null.
  - `confidence` is 0.0–1.0 for how sure you are of the speaker (use 1.0 for null).
- `characters` lists the people who speak or think in this slice, each as
  `{name, aliases, gender, age_hint, description}`. Set unknown fields to null; never use
  `id` or `canonical_name` keys. Invent nothing.
- `thoughts` is an array of `{candidate_id, is_thought, thinker, confidence}`, one entry per
  thought candidate listed below (see "Thought candidates").

A block usually has a single speaker for its dialogue. If a block genuinely mixes two
speakers, name the one who speaks the most.

### Example

Given:

```
[ch004_b0003]
"You are too kind," said Mr. Bennet, bowing.
[ch004_b0004]
Elizabeth made no reply, and walked on. She will never forgive me, he thought.
```

With thought candidate `ch004_b0004:44` -> "She will never forgive me,"

Return:

```json
{
  "blocks": [
    {"block_id": "ch004_b0003", "speaker": "Mr. Bennet", "confidence": 0.97},
    {"block_id": "ch004_b0004", "speaker": null, "confidence": 1.0}
  ],
  "characters": [
    {"name": "Mr. Bennet", "aliases": [], "gender": "male", "age_hint": null, "description": null},
    {"name": "Elizabeth", "aliases": [], "gender": "female", "age_hint": null, "description": null}
  ],
  "thoughts": [
    {"candidate_id": "ch004_b0004:44", "is_thought": true, "thinker": "Mr. Bennet", "confidence": 0.9}
  ]
}
```

## Rules

1. **One entry per block**, using each block's exact `block_id`.
2. **Resolve to who is actually speaking** — follow the dialogue tags (`said Mr. Bennet`),
   pronouns, and the back-and-forth of a conversation. The speaker is rarely the person
   being addressed.
3. **Prefer registry names.** If the speaker is already in the registry below, reuse that
   exact `name`. Add new speakers under `characters`.
4. **Null for narration.** A block with no spoken dialogue gets `speaker: null`.

## Thought candidates

Each candidate below is an italicized passage the system already located. For EACH one,
return a `thoughts` entry with its exact `candidate_id`:

- Set `is_thought` to `true` ONLY if the passage is a character's genuine unspoken interior
  monologue (a silent thought). Set it to `false` if the italics are mere emphasis, a book /
  song / ship title, a foreign phrase, a letter or sign being read, or anything spoken
  aloud. When unsure, prefer `false`.
- When `is_thought` is `true`, set `thinker` to the name of the character doing the
  thinking (reuse a registry name where possible). Cue verbs nearby — *thought*, *wondered*,
  *mused*, *asked herself* — are good evidence for the thinker but are not required. If you
  cannot name the thinker, set `is_thought` to `false`.
- `confidence` is 0.0–1.0. Never invent a candidate_id that is not listed; only return
  verdicts for the ids given.

Candidates:

{thought_candidates}

## Known characters so far (registry)

```json
{registry_json}
```

## Preceding context (reference only — do NOT attribute these)

{context_blocks}

## Blocks to attribute

{blocks}
